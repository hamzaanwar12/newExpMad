// app.config.ts
{/*

  import { Env } from '@/env';
  import { ConfigContext, ExpoConfig } from '@expo/config';
  
  export default ({ config }: ConfigContext): ExpoConfig => {
    return {
      ...config,
      name: Env.EXPO_PUBLIC_SUPABASE_URL, // Using the public URL here
      slug: Env.EXPO_PUBLIC_SUPABASE_URL.split('/').pop() || "default-slug", // Ensure slug is a string
    };
  };
  */}
